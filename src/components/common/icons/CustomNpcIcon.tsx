import AccountCircleIcon from "@mui/icons-material/AccountCircle";
import { styled } from "@mui/system";

const CustomNpcIcon = styled(AccountCircleIcon)({
  color: "inherit",
  fontSize: "inherit",
});

export default CustomNpcIcon;
